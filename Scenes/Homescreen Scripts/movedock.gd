#extends Sprite


# Declare member variables here. Examples:
# var a = 2
# var b = "text"
#var t = 50

# Called when the node enters the scene tree for the first time.
#func _process(delta):
	
#nready var timer = get_node("/root/Control/HBoxContainer/Sprite2/Timer")
#func _process(delta):
	#if position.y >= 20:
		#position = Vector2(0,30)
		#print("test")
#func _process(delta):
	#move_toword(100)


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass




#func _on_Timer_timeout():
	#print("test2")
	#while position.y >= 50:
		#position.y -= 3
		#t -= 1
		#print(position.y)
#
